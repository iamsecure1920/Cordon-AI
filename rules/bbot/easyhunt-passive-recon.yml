# BBOT preset: passive-only attack surface mapping.
#
# Everything here reads public sources — certificate transparency, passive DNS,
# public code search. No requests are sent to the target's own infrastructure
# beyond DNS resolution, which is why Cordon exposes it as a passive tool that
# runs without an approval prompt.
description: Cordon passive attack-surface mapping (no active probing)

flags:
  - passive
  - subdomain-enum

modules:
  - anubisdb
  - certspotter
  - crt
  - dnscommonsrv
  - digitorus
  - hackertarget
  - otx
  - rapiddns
  - sitedossier
  - subdomaincenter
  - wayback

output_modules:
  - json

config:
  # Cordon overwrites scope.whitelist / scope.blacklist from scope.yaml at
  # launch time. Anything set here is a floor, never a ceiling.
  dns:
    minimal: false
  web:
    user_agent: "Cordon-AI/2.0 (authorized-testing)"
  scope:
    report_distance: 0
